# Environment / secrets
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
.venvs/
venv/
env/
# uv resolves this on the fly for local `uv run`; the project pins deps in
# pyproject.toml, so the lockfile is not tracked.
uv.lock

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.cache/

# ADK / agent runtime artifacts
.adk/
*.session.json

# Local Phoenix observability instance (traces, pid/log files)
deployment/phoenix/.data/
deployment/phoenix/.run/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
/creds.json
# Prepared SQL cache snapshots, one directory per DS_UTILS_RUN_MODE
data/local/
data/dev/
data/qa/
data/perf/
data/preprod/
data/prod/

# Runtime human-feedback log (durable annotation store; not source)
feedback_data/
.venv-test/
